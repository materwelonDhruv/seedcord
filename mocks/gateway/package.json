{
    "name": "@seedcord/mock-gateway",
    "type": "module",
    "version": "0.2.33-next.0",
    "engines": {
        "node": ">=24.11"
    },
    "private": true,
    "description": "Gateway-transport mock bot for demonstration and testing",
    "repository": {
        "type": "git",
        "url": "https://github.com/seedcord/seedcord.git",
        "directory": "mocks/gateway"
    },
    "scripts": {
        "dev": "seedcord dev",
        "build": "seedcord build",
        "start": "pnpm build && node --enable-source-maps dist/index.mjs",
        "codegen": "seedcord codegen",
        "clean": "rm -rf dist",
        "lint": "eslint 'src/**/*.{ts,tsx}' --cache --no-error-on-unmatched-pattern",
        "lint:fix": "eslint 'src/**/*.{ts,tsx}' --fix --cache --no-error-on-unmatched-pattern",
        "fmt": "prettier --write 'src/**/*.{ts,tsx,json,md}' --cache --no-error-on-unmatched-pattern",
        "fmt:check": "prettier --check 'src/**/*.{ts,tsx,json,md}' --cache --no-error-on-unmatched-pattern",
        "tc": "tsgo --noEmit",
        "tc:watch": "tsc --noEmit --watch"
    },
    "peerDependencies": {
        "typescript": "catalog:peer"
    },
    "dependencies": {
        "@discordjs/builders": "catalog:deps",
        "@seedcord/gateway": "workspace:^",
        "@seedcord/plugin-mongoose": "workspace:^",
        "discord.js": "catalog:peer",
        "envapt": "catalog:peer",
        "mongoose": "catalog:peer"
    },
    "devDependencies": {
        "@seedcord/eslint-config": "workspace:^",
        "@seedcord/tsconfig": "workspace:^",
        "seedcord": "workspace:^"
    }
}
