---
'@seedcord/errors': minor
---

One `ManifestEntryWrongClass` replaces `InteractionRouteExportMissing`, `SubscriberRouteNotASubscriber`, and `RouteModuleLoadFailed`. It names the manifest array and the class listed in it.
