---
'@seedcord/errors': minor
---

`ManifestEntryWrongClass` and `ManifestEntryNoRoutes` replace `InteractionRouteExportMissing`, `SubscriberRouteNotASubscriber`, and `RouteModuleLoadFailed`. Each names the manifest array and the class listed in it.
